# Development Profile Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/fbcs_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=fbcs_user
spring.datasource.password=Fbcspassword_2026
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# H2 In-Memory database setup can be configured here if preferred during local quick tests:
# spring.datasource.url=jdbc:h2:mem:fbcs_db;DB_CLOSE_DELAY=-1
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Debug logs
logging.level.org.springframework.security=DEBUG
logging.level.com.feedback.feedbacksystem=DEBUG
